# 黎明岛 / Dawn Island — 新会话交接单（2026-08-01 20:15 快照）

> **用途**：新开会话继续执行本项目时的交接底稿。先整段粘贴「A 节」，再让新会话自读
> `OPS_MANUAL.zh-CN.md`（总运维手册，含红线/三步法/架构/部署全量细节）与本文档。
> 本单是**带日期快照**（当前 HEAD、待发布改动、已知问题、后续计划），与手册互为补充。
>
> **本次更新要点（19:05 → 20:15）**：完成「移除 Obsidian vault / sync 单向依赖」架构简化，
> 全项目 13 份文档去陈旧化；改动集已落盘 **但尚未 push**，见 B.2 节。

---

## A. 给新会话的整段粘贴文本

```
你正在接手「黎明岛 / Dawn Island」(https://www.limingdao.com) 项目的后续运维。

第一步：完整阅读仓库根目录 `OPS_MANUAL.zh-CN.md`（总运维交接手册，按 §0 定位必读章节）
和 `新会话交接-2026-08-01.md`（带日期快照的现状交接单：待发布改动 + 当前问题 + 后续计划）。

项目速记（与手册一致，红线优先）：
- 性质：纯 GEO/SEO 用途的**双语(zh/en)静态站**，Astro 5 + Content Collections + Zod，
  GitHub→Vercel 部署，**无交互功能**（用户硬性约束；第三方广告脚本除外，变现为建站目的）。
- 内容真源 = 项目内 `src/content/`（即站点构建输入），直接编辑即上线。
  **没有独立 Obsidian vault 根目录，没有 vault:seed/vault:sync 同步脚本**（2026-08-01 移除该层）。
  想要 UI 就用 Obsidian 直接 `Open folder as vault` 打开 `src/content` 文件夹当编辑器用。
- 发布控制：账号用 `draft: true` 暂不上线；分类/平台/news/guides 无草稿闸门，
  **有文件即上线，删文件即下线**。旧的 `type`/`status`/`publish` 字段 schema 里已不存在。
- 红线（不可擅自）：① 不编造粉丝/互动/增长/排名数据（查不到就留空，页面显示"—"）；
  ② 不改已发布实体的 slug；③ 不私自 push 生产；④ Cloudflare「托管 robots.txt」开关必须
  保持关闭（误开会覆盖仓库版、屏蔽 AI 爬虫、断 GEO）；⑤ 任何改/生成/删文件前走三步法
  （提方案→等我看→等我确认）。
- 推送命令：`git -c credential.helper= -c credential.helper=wincred push origin main`（Windows wincred）。
- 本地 `npm run build` 若报 EXIT=1：多为沙箱 safe-delete 钩子误报，看 Astro 的 `✓ Completed`
  与页面数判定成功（当前 441 页），Vercel 不受影响。

当前状态（2026-08-01 20:15）：
- 生产 HEAD = 3e4f296；**工作树有 17 个文件的架构简化改动集未 push**（详见交接单 B.2）。
- 169 账号（全双语正文 + 双语 facts/faq）+ 17 分类 + 10 平台 + 24 篇长文指南(zh/en 各 12)
  + 4 篇 link 型指南 + 4 news；构建 441 页。
- 全站英文页 0 中文（本地 220 个 en 页扫描 + 生产实抓均验证）。
- AdSense 已接入（ads.txt + head 脚本，Auto ads 机制），后台跟进已搁置（用户自行处理）。

我的具体需求是：[在此写本次任务]
```

---

## B. 当前状态快照

### B.1 基础数据（20:15 实测）

| 项 | 值 |
|----|----|
| 生产 HEAD | `3e4f296`（fix(vault): resolve summary round-trip defect, change 6） |
| 已 push 链路 | `7a0000b`(AdSense) → `17c332c`(Batch2 去薄化 169 账号) → `571d369`(29 页中文清理+双语 i18n) → `3e4f296`(改动6 修复) |
| 工作树状态 | **17 文件改动未提交**（13 改 + 3 删 + 1 新增），+216 / −913 行 |
| accounts | 169（`draft: true` 数量 = **0**，即全部上线） |
| categories / platforms | 17 / 10 |
| guides | **article 型 24**（`guides/zh` 12 + `guides/en` 12）+ **link 型 4**（`guides/` 根目录） |
| news | 4 |
| 构建 | **441 页**，`✓ Completed in 5.66s`，`sitemap-index.xml` 正常产出，Exit Code 0 |
| scripts/ | 仅 `dethin-accounts.py`（同步三件套已删） |
| package.json scripts | 仅 `dev` / `build` / `preview`（无 vault 命令） |
| 英文页中文 | **0**（本地 220 页扫描 + 生产实抓 account/guide/category/exit/home 全 0） |
| AdSense | 代码就绪（Auto ads 机制），**后台开关与过审跟进已搁置**（用户自行） |
| 监控 | GA4 `G-WJ8ZP9FSE9` + MS Clarity `kn4x488ytp`（BaseLayout 全局注入） |

### B.2 ⚠️ 待发布改动集（已落盘 · 未 commit · 未 push）

**主题**：移除 Obsidian vault / `vault:sync` 单向依赖，确立 `src/content/` 为唯一真源；全项目文档去陈旧化。

**代码层（4 项）**
| 文件 | 动作 |
|------|------|
| `scripts/seed-vault-from-site.mjs` | **删除** |
| `scripts/sync-vault-to-site.mjs` | **删除** |
| `scripts/content-utils.mjs` | **删除**（仅同步三件套依赖） |
| `package.json` | 移除 `vault:seed` / `vault:sync` 两条 script |
| `scripts/dethin-accounts.py` | 输出路径重定向到 `src/content/accounts/`（原写 vault） |

**文档层（12 份，全部对齐新模型）**
`README.md`、`README.zh-CN.md`、`USAGE.md`、`USAGE.zh-CN.md`、`AI_OPERATIONS.zh-CN.md`、
`UPGRADE.md`（新增 §11 Content Source）、`UPGRADE.zh-CN.md`、`OPS_MANUAL.zh-CN.md`（§8.2/§8.3/§13 发布控制重写）、
`OBSIDIAN_WORKFLOW.zh-CN.md`（整篇重写为「用 Obsidian 管理 src/content」）、
`ARCHITECTURE.zh-CN.md`（**整篇重写**：原文竟仍是转型前的「工具目录 / getTools / src/content/tools」模型，已改为 accounts 模型）、
`代码改动记录.md`（追加本次台账）、本交接单。

**发布命令（等用户确认）**
```bash
git add -A
git commit -m "remove Obsidian vault/sync layer; src/content is single source of truth; de-stale all docs"
git -c credential.helper= -c credential.helper=wincred push origin main
```

---

## C. 架构简化说明（2026-08-01，新会话必读）

**旧模型（已废弃）**：独立 Obsidian vault 根目录 `E:\Obsidian\www.limingdao.com\` ↔ `npm run vault:seed`（站点→库）/ `npm run vault:sync`（库→站点）双向脚本；库卡片用 `type` / `status: approved` / `publish: true` 作控制字段，sync 时剥离。

**新模型（现行）**：
1. **唯一真源 = `src/content/`**，Astro 构建时直接读取，无中间层。
2. Obsidian 只是可选的编辑器 UI——`Open folder as vault` 打开 `D:\project\codex\toolify\src\content` 即可（`.obsidian/` 已在 `.gitignore` 中忽略）。
3. **发布控制**：账号 `draft: true` = 暂不上线（`content.config.ts` 第 130 行 `draft: z.boolean().optional().default(false)`，`directory.ts` 的 `load()` 过滤，不进路由/sitemap）；其余集合有文件即上线，删文件即下线。
4. 旧 `type` / `status` / `publish` 控制字段**在 schema 中完全不存在**，写了也无效。
   - 注意区分：`type: "content"` 是 Astro loader 配置；platform 的 `type` 是内容形态枚举（short-video/video…）；guide 的 `kind: link|article` 是形态区分——**都不是发布闸门**。

**日常流程**：改 `src/content/**.md` → `npm run build`（Zod 拦截错误数据）→ 更新 `代码改动记录.md` → 等用户确认 → wincred push → 生产实抓验证。

---

## D. 当前已知问题 / 未决事项

| # | 问题 | 状态 | 处置建议 |
|---|------|------|----------|
| 1 | B.2 改动集未 push | **待用户确认** | 用 B.2 的三条命令发布；push 后回填本单 HEAD 与已 push 链路 |
| 2 | tags/contentStyle 英文页用 **safe-filter v1**（ASCII 过滤、中文标签整条隐藏），非完整双语化 | 已生效·可选升级 | 如需 en 页显示翻译标签：约 750 词翻译 + 在 `src/content.config.ts` 增双语标签字段 + 改详情页渲染逻辑（**不再涉及任何同步脚本**） |
| 3 | 本地 `npm run build` 偶报 EXIT=1 / sitemap 不写出 | 已知沙箱行为 | 沙箱 `genie-safe-delete` 钩子清理 `dist/_noop-middleware.mjs` 失败所致，会中断 `astro:build:done`（sitemap 在此写入）。**20:15 本次构建未触发，sitemap 正常产出**。判定标准：看 `✓ Completed` + 页面数，勿当代码错误排查；Vercel 无此钩子 |
| 4 | `git status` 显示 `origin/main: gone`、`ls-remote`/`fetch` 返回空 | 已知凭据坑 | WorkBuddy 自带 PortableGit 无 credential-manager；仅 push 需用 wincred 覆盖命令，push 本身正常 |
| 5 | 账号指标（followerCount 等）为可选字段 | 约定非缺陷 | 查不到就留空，绝不编造（红线①） |
| 6 | 抖音/小红书直连 profile 需 sec_uid/用户ID，公开难稳定获取 | 约定 | 用 B 站 space 直连（`https://space.bilibili.com/{UID}/`）或平台官方搜索 URL 兜底 |
| 7 | 早期 ai-content 老账号用自定义 slug 型 platformId（bili-li-mu 等） | 历史遗留·不回改 | 新账号统一：B站=数字 UID；抖音=URL 编码中文名搜索 |
| 8 | git CRLF 警告（core.autocrlf=true 无 .gitattributes） | 良性 | 无需处理 |
| 9 | guides 曾有 4 篇空模板草稿（ten-ways-start 等） | **已删除**（2026-08-01） | 勿在 `src/content/guides/` 重新创建空模板占位 |

---

## E. 后续计划（按优先级，待用户拍板）

### E1. 内容侧（增长核心）
- **英文指南独立选题扩写**：用户已授权 AI 按英文搜索意图独立选题（**非中文翻译件**），英文单位流量 AdSense 收益通常数倍于中文，是变现效率最高资产。当前仅 12 篇 en 长文。
- **新账号采集**：wechat-article-search + WebSearch 反查核实，直接写进 `src/content/accounts/`（真实账号、不编数据）。
- **可选**：tags/contentStyle 完整双语化（约 750 词翻译，见 D2）。

### E2. 变现侧（用户自行，已搁置跟进）
- 用户去 AdSense 后台开启 Auto ads + 等过审 → 全站自动插广告，**无需改代码**。
- 过审后：用户在后台创建手动广告单元，把 `data-ad-slot` ID 给我，再按三步法插入（指南文章页中部矩形、列表/频道页顶部 leaderboard）。

### E3. 技术侧
- 架构保持稳定：页面只经 `src/lib/directory.ts` 读数据，扩容不破坏 URL/SEO。
- 发 article 型指南直接写 `src/content/guides/{zh,en}/<guideId>.md`，无中间脚本。

### E4. 运维侧（日常）
- 每批内容改动：改 `src/content/` → `npm run build`（看 ✓ Completed）→ 更新 `代码改动记录.md` → 等用户确认 → wincred push → 生产实抓验证。
- 代码级修改必须同步记入 `代码改动记录.md`（人类可看台账，用户硬性要求；AI memory 仅作索引，不能替代）。

---

## F. 关键操作速查

```bash
# 推送（唯一正确姿势，wincred 覆盖）
git -c credential.helper= -c credential.helper=wincred push origin main

# 构建验证（看 ✓ Completed + 页面数，EXIT 码可能被沙箱钩子污染）
npm run build

# 内容真源即 src/content/，直接编辑；想要 UI 就在 Obsidian 里
# Open folder as vault → D:\project\codex\toolify\src\content
# （无 vault:seed / vault:sync，这两条命令已不存在）

# 批量账号去薄化生成器（基于 frontmatter 真实字段扩写，不编造）
python scripts/dethin-accounts.py   # FORCE=1 重生成；BLOCKLIST 保护手写账号

# 英文页 0 中文扫描（可选，验证用）
# 对 dist/en/**/*.html 扫 CJK 正则 [\u3400-\u4dbf\u4e00-\u9fff\uf900-\ufaff]
```

---

## G. 关键文件索引

| 文件 | 作用 |
|------|------|
| `OPS_MANUAL.zh-CN.md` | **总运维交接手册**（自包含：红线/三步法/架构/部署/故障排查/新会话提示词；含手动运维 §6、AI 运维 §7、技术升级 §3·§11、GEO·SEO 报告 §15） |
| `ARCHITECTURE.md` / `ARCHITECTURE.zh-CN.md` | 架构说明（accounts 模型、数据流、路由；zh 版 2026-08-01 重写） |
| `USAGE.zh-CN.md` / `USAGE.md` | 字段与操作全量参考 |
| `OBSIDIAN_WORKFLOW.zh-CN.md` | 用 Obsidian 当编辑器管理 `src/content` 的工作流（**非交接文档**） |
| `AI_OPERATIONS.zh-CN.md` | AI 运营纪律与命令表 |
| `UPGRADE.zh-CN.md` / `UPGRADE.md` | 技术升级路线与阈值 |
| `代码改动记录.md` | 代码改动台账（凡代码级修改必须记录 diff+验证，人类可看） |
| `scripts/dethin-accounts.py` | 账号页去薄化批量生成器（唯一保留脚本） |
| （已删除）`scripts/seed-vault-from-site.mjs` / `sync-vault-to-site.mjs` / `content-utils.mjs` | 原 Obsidian 同步三件套，2026-08-01 移除 |
| `src/content.config.ts` | Zod schema（账号 `draft` 闸门；geo 支持 facts_zh/en、faq_zh/en 六字段） |
| `src/lib/directory.ts` / `i18n.ts` / `jsonld.ts` | 数据读取（含 draft 过滤）/ 双语 i18n / 结构化数据生成 |
| `src/layouts/BaseLayout.astro` | 全局注入（canonical/og/GA4/Clarity/AdSense 脚本） |
| `src/pages/[lang]/exit.astro` / `submit.astro` | 外链中转页 / 推荐页（已 is:inline define:vars 双语化） |

---

*本单由 AI 于 2026-08-01 20:15 生成，全部数据经当场实测（git / find / npm run build）；下次重大变更或 push 后应更新 B 节。*
