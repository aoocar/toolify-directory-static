---
import { localizedPath, type Lang } from "@/lib/i18n";
import type { Category } from "@/lib/types";

type Props = {
  category: Category;
  lang: Lang;
  count?: number;
};

const { category, lang, count } = Astro.props;
---

<a class="category-card" href={localizedPath(lang, `/categories/${category.slug}`)}>
  <span class="category-icon">{category.icon}</span>
  <div>
    <h3>{category.name[lang]}</h3>
    <p>{category.description[lang]}</p>
    {count !== undefined && <small>{count} {lang === "zh" ? "个账号" : "creators"}</small>}
  </div>
</a>
